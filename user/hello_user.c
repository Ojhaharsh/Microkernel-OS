// Placeholder user program source for Week 5 (user mode + syscalls)
int main(void) {
    // In Week 5, this will be compiled as a user program and loaded by the kernel's ELF loader
    return 0;
}
